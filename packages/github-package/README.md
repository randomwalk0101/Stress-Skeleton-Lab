# Stress Skeleton Lab Package

This package exists so the repository publishes a GitHub Package entry under the
`Packages` section on GitHub.

It is separate from the browser release assets and only serves as a registry
entry for the project.
